gdata_app_headers = files(['gdata-app-categories.h'])

install_headers(
  gdata_app_headers,
  install_dir: gdata_includedir / 'gdata/app'
)

sources += files(['gdata-app-categories.c'])

headers += gdata_app_headers
